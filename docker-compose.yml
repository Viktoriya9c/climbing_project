services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - ./input:/app/input
      - ./outputs:/app/outputs
      - ./models:/app/models
      - ./state.json:/app/state.json
    restart: unless-stopped
